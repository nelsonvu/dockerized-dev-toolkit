# Docker Toolkit: Setting Up Redis

## Installation

```bash
# Create folder to store Redis data
mkdir data

# Run Redis container
docker-compose up -d
```

## Result

The Redis service is running on port 6379
